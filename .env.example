# Better Auth session signing secret (dev only — the real one is a wrangler secret)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NUXT_AUTH_SECRET=dev-only-insecure-secret
